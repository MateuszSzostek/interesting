import styled, { css } from "styled-components";
import { ITypography } from "./types";
import { Colors, FontSizes, BreakPoints } from "../../../global-styles";

/*
const variantStyles = (variant: H1Variant = "default") =>
    ({
        default: css`
            color: ${Colors._gray00};
            font-size: ${FontSizes._px30};

            @media only screen and (max-width: ${BreakPoints._mobile}) {
            }
        `,
    }[variant]);
    */

export const StyledTypography = styled.p``;
